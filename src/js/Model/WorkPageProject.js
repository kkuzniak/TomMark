export default class WorkPageProject {
    constructor(id, linkName, mainHeader, firstSubheader, secondSubheader, imageSrc, nextProjectLink) {
        this.id = id;
        this.linkName = linkName;
        this.mainHeader = mainHeader;
        this.firstSubheader = firstSubheader;
        this.secondSubheader = secondSubheader;
        this.imageSrc = imageSrc;
        this.nextProjectLink = nextProjectLink;
    }
}