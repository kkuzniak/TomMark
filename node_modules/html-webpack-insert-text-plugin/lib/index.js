"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var HtmlWebpackPlugin = _interopRequireWildcard(require("html-webpack-plugin"));

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = Object.defineProperty && Object.getOwnPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : {}; if (desc.get || desc.set) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } } newObj.default = obj; return newObj; } }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var filter = function filter(assets, parent, target) {
  return assets.filter(function (item) {
    return item.parent === parent;
  }).filter(function (item) {
    return !target || item.target === target;
  }).map(function (item) {
    return item.text;
  }).join('\n');
};

var insert = function insert(html, parent, content) {
  var tag = parent === 'body' ? `</${parent}>` : `<${parent}>`;
  var tagIndex = html.indexOf(tag);
  return [html.slice(0, tagIndex), content, html.slice(tagIndex)].join('');
};

var HtmlWebpackInsertTextPlugin = function HtmlWebpackInsertTextPlugin(config) {
  var _this = this;

  _classCallCheck(this, HtmlWebpackInsertTextPlugin);

  _defineProperty(this, "assets", void 0);

  _defineProperty(this, "addAssets", function (htmlPluginData, parent) {
    var content = filter(_this.assets, parent, htmlPluginData.plugin.options.filename);
    htmlPluginData.html = insert(htmlPluginData.html, parent, content);
  });

  _defineProperty(this, "apply", function (compiler) {
    compiler.hooks.compilation.tap('HtmlWebpackInsertTextPlugin', function (compilation) {
      var hooks = HtmlWebpackPlugin.getHooks(compilation);
      hooks.beforeEmit.tap('HtmlWebpackInsertTextPlugin', function (htmlPluginData) {
        _this.addAssets(htmlPluginData, 'head');

        _this.addAssets(htmlPluginData, 'body');

        return htmlPluginData;
      });
    });
  });

  if (config.length === 0) {
    throw new TypeError('Need asset definitions');
  }

  this.assets = config;
};

exports.default = HtmlWebpackInsertTextPlugin;